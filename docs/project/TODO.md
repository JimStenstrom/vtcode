Propose a technical implementation for a chunked file preview feature designed to handle large files more efficiently. Currently, the preview functionality always displays content starting from the beginning of the file, which is impractical for large datasets. Modify the design to allow users to specify a segment of the file using arguments such as `offset` and `length`. Please provide a detailed plan covering the tool's interface updates, the logic for seeking to a specific byte offset, and the method for reading and returning the requested chunk of data.
